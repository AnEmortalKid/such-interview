shh cerner_2^5_2017

such candles much arr

  very max is -1
  much very i as 0 next i smaller arr.length next i more 1
    very localMax is arr[i]
    rly localMax bigger max
      max is localMax
    wow
  wow
  
  very candleCount is 0
  much very i as 0 next i smaller arr.length next i more 1
    very val is arr[i]
    rly val is max
      candleCount is candleCount+1
    wow
  wow

wow candleCount