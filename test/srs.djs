such srsly much f input expected
  very msg is f.name+'('+input+') === ' + expected + '?'

  very actual is plz f with input
  rly actual not expected
    console dose log with msg
    throw 'No! Got: '+ actual
  but
    msg is msg + ' Yes!'
  wow
  
  console dose log with msg
wow